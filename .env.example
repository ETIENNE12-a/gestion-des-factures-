# Environment variables example for development
# Copy this file to `.env` and fill the real secret values. Never commit the real `.env`.

# Clerk (auth)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Database (Prisma). For development we use SQLite; in production use Postgres/MySQL etc.
DATABASE_URL="file:./dev.db"

# Public URLs for Clerk routes (used by frontend)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Example: optional runtime settings
# NODE_ENV=development
# PORT=3000
